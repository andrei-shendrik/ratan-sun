#name: Manual production deploy on solar server
#
#
#on:
#  workflow_dispatch: # триггер workflow_dispatch запуск скрипта вручную кнопкой в GitHub
#    inputs:
#      version:
#        description: 'Version tag to deploy (e.g., v1.0.1)'
#        required: true
#        default: 'latest'
#
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest # ОС внутри самого гитхаб
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#        with:
#          ref: ${{ github.event.inputs.version }} # Скачиваем код конкретного тега
#
#      - name: Login to GitHub Container Registry
#        uses: docker/login-action@v3
#        with:
#          registry: ghcr.io
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#
#      # Собираем и пушим Django
#      - name: Build and push Django API
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          file: ./apps/djangoapi/Dockerfile
#          push: true
#          tags: ghcr.io/${{ github.repository_owner }}/myproject-djangoapi:${{ github.event.inputs.version }}
#
#      # Аналогично собираешь React (тут опускаю для краткости)
#      # ...
#
#  deploy-to-server:
#    needs: build-and-push # Ждем, пока соберутся образы
#    runs-on: ubuntu-latest
#    steps:
#      - name: SSH to Production and Deploy
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.SERVER_IP }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            # Переходим в папку проекта (вендорный подход)
#            cd /opt/mycompany/myproject
#
#            # Логиним сервер в твой GitHub Registry, чтобы он мог скачать приватные образы
#            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
#
#            # В docker-compose.yml на сервере нужно обновить теги (можно через .env файл)
#            echo "APP_VERSION=${{ github.event.inputs.version }}" > .env.version
#
#            # Скачиваем новые собранные образы
#            docker compose pull
#
#            # Перезапускаем контейнеры без даунтайма (насколько это возможно)
#            docker compose up -d --remove-orphans

#- name: SSH to Server and Deploy
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.SERVER_IP }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            # 1. Переходим в папку проекта на сервере
#            cd /opt/mycompany/myproject
#
#            # 2. Магия Linux (sed): Ищем в файле .env строку, которая начинается
#            # с PROJECT_VERSION= и заменяем всё после равно на наш новый git-тег.
#            # Если переменной там еще нет, добавляем её в конец файла.
#            if grep -q "^PROJECT_VERSION=" .env; then
#              sed -i "s/^PROJECT_VERSION=.*/PROJECT_VERSION=${{ github.ref_name }}/" .env
#            else
#              echo "PROJECT_VERSION=${{ github.ref_name }}" >> .env
#            fi
#
#            # 3. Скачиваем новые образы (Докер уже сам прочитает новую версию из .env)
#            docker compose pull
#
#            # 4. Перезапускаем контейнеры
#            docker compose up -d --remove-orphans